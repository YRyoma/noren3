<h1>General#index</h1>
<% @articles.each do |article| %>
<p><%= link_to article.title, general_path(article.id) %></p>
<p><%= link_to article.member.name, member_path(article.member.id) %></p>


<% end %>
